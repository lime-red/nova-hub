__pycache__/
*.py[cod]
venv/
.env
data/
*.db
*.log
.pytest_cache/
.DS_Store
config.toml*
labs.sh
*.ipynb
dosemu_configs/bre.conf
node_modules/
frontend/dist/
# Incremental typecheck caches. They are rewritten by every `pnpm run build`,
# which would leave a freshly deployed tree dirty and trip the deploy guard.
*.tsbuildinfo
._*
deploy/inventory.yml
.claude

# Skills are documentation meant to be shared, unlike the rest of .claude/
!.claude/
!.claude/skills/
!.claude/skills/**
._*
